import { Layout } from "antd";
const { Header } = Layout;

const HeaderMain = (props) => {
  return (
    <div>
      <div className="search-bar"></div>
    </div>
  );
};

export default HeaderMain;
